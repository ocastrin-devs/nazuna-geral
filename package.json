{
  "name": "nazuna-site",
  "version": "1.0.0",
  "description": "Site da Nazuna",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"  
    // Se o seu ponto de entrada for 'handler.js', use "node handler.js"
  },
  "dependencies": {
    // Liste suas dependências aqui (ex: express, cors, etc.)
  }
}
